# Music_Thoughts — зависимости
# Установка:  pip install -r requirements.txt

# --- Веб-сервер (ядро приложения) ---
Flask>=3.0.0
Werkzeug>=3.0.0
Jinja2>=3.1.0
itsdangerous>=2.1.0
click>=8.1.0
MarkupSafe>=2.1.0
blinker>=1.7.0

# --- Десктопная обёртка (отдельное окно Music_Thoughts.exe) ---
pywebview>=5.0

# --- Сборка автономного .exe ---
pyinstaller>=6.0
